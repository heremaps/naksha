// This will be exposed
// - in JavaScript at the namespace: naksha.base.fn.{name}
// - jn Java at the class naksha.base.fn.NakshaBaseFnKt.{name}
package naksha.base.fn

import naksha.base.Platform.Platform_C.forKClass
import naksha.base.PlatformType

/**
 * The package name `naksha.base.fn`.
 * @since 3.0
 */
const val PACKAGE_NAME = "naksha.base.fn"

/**
 * The [PlatformType] of [Fn].
 * @since 3.0
 */
val Fn_TYPE = forKClass(Fn::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn0].
 * @since 3.0
 */
val Fn0_TYPE = forKClass(Fn0::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn1].
 * @since 3.0
 */
val Fn1_TYPE = forKClass(Fn1::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn2].
 * @since 3.0
 */
val Fn2_TYPE = forKClass(Fn2::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn3].
 * @since 3.0
 */
val Fn3_TYPE = forKClass(Fn3::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn4].
 * @since 3.0
 */
val Fn4_TYPE = forKClass(Fn4::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn5].
 * @since 3.0
 */
val Fn5_TYPE = forKClass(Fn5::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn6].
 * @since 3.0
 */
val Fn6_TYPE = forKClass(Fn6::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn7].
 * @since 3.0
 */
val Fn7_TYPE = forKClass(Fn7::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn8].
 * @since 3.0
 */
val Fn8_TYPE = forKClass(Fn8::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fn9].
 * @since 3.0
 */
val Fn9_TYPE = forKClass(Fn9::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx0].
 * @since 3.0
 */
val Fx0_TYPE = forKClass(Fx0::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx1].
 * @since 3.0
 */
val Fx1_TYPE = forKClass(Fx1::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx2].
 * @since 3.0
 */
val Fx2_TYPE = forKClass(Fx2::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx3].
 * @since 3.0
 */
val Fx3_TYPE = forKClass(Fx3::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx4].
 * @since 3.0
 */
val Fx4_TYPE = forKClass(Fx4::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx5].
 * @since 3.0
 */
val Fx5_TYPE = forKClass(Fx5::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx6].
 * @since 3.0
 */
val Fx6_TYPE = forKClass(Fx6::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx7].
 * @since 3.0
 */
val Fx7_TYPE = forKClass(Fx7::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx8].
 * @since 3.0
 */
val Fx8_TYPE = forKClass(Fx8::class).withPackageName(PACKAGE_NAME)

/**
 * The [PlatformType] of [Fx9].
 * @since 3.0
 */
val Fx9_TYPE = forKClass(Fx9::class).withPackageName(PACKAGE_NAME)


