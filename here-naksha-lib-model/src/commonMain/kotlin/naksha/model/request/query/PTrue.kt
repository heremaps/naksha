package naksha.model.request.query

/**
 * Dummy implementation if [IPropertyQuery] that always evaluates to [true]
 * Useful for replacing nested property query, should be avoided otherwise
 */
object PTrue: IPropertyQuery